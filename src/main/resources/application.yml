spring:
    application:
        name: configuration-server

server:
    port: ${SERVER_PORT:8888}
spring:
    cloud:
        config:
            server:
                git:
                    uri: https://github.com/BrunoCaetanoDev/configuration-repository

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always

eureka:
  client:
    registryFetchIntervalSeconds: 5
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://localhost:8761/eureka
  instance:
    hostname: configuration-repository